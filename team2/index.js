const mul = (a, b) => {
  return a * b;
};

const summary = (a, b) => {	// first comment
  return a + b;
};

const div = (a, b) => { // divison for team projekt madebyKZ
  return a / b;
};

module.exports = {
  summary: summary,
  div: div,
  mul: mul
};
